// third-party libraries
import { expect } from 'chai';

// modules
import app from './server';

// test suites
describe('Server', () => {
  it('start & serve app', () => {
    expect(true).to.equal(true);
  });
});
