// libraries
import assert from 'assert';
import { expect } from 'chai';

// modules
import app from './server';

/**
 * @desc
 */
describe('Server', () => {
  it('start & serve app', () => {
    expect(true).to.equal(true);
  });
});
